<section class="container mx-auto px-4 py-12 text-center">

    <h1 class="text-4xl font-bold mb-4">Waarom overstappen naar FestCoin?</h1>
    <p class="text-xl text-gray-700 mb-8 max-w-2xl mx-auto"><%= sub %></p>

    <!-- stappen -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-2 gap-8">
        <% daarom.forEach(card => { %>
            <%- include("Cards/daaromCard", card) %>
        <% }) %>
    </div>
</section>